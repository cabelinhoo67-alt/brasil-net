<?xml version="1.0" encoding="utf-8"?>
<!-- Card minimalista do overlay flutuante: logo "B", ping em tempo real e
     bolinha de status. Preto fosco absoluto, sem nenhum azul. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlay_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/overlay_card_bg"
    android:paddingStart="10dp"
    android:paddingEnd="14dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:elevation="8dp">

    <TextView
        android:id="@+id/overlay_logo"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/overlay_logo_bg"
        android:gravity="center"
        android:text="B"
        android:textColor="#39FF14"
        android:textSize="16sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/overlay_ping"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="-- ms"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/overlay_status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Conectado"
            android:textColor="#8A8A8A"
            android:textSize="10sp" />
    </LinearLayout>

    <View
        android:id="@+id/overlay_status_dot"
        android:layout_width="9dp"
        android:layout_height="9dp"
        android:layout_marginStart="10dp"
        android:background="@drawable/overlay_status_dot" />

</LinearLayout>
